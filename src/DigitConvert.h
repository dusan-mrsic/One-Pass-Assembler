#ifndef DIGITCONVERT_H
#define DIGITCONVERT_H
#include <string>

using namespace std;

string intToHex(int num);

string makeTwoBytes(string hex);

string makeWord(string first, string second, string thrid, string fourth, string fifth, string sixst, string seventh);

#endif